declare namespace VIRTRU_DSP_COP_ENV {
  const platformEndpoint: string;
  const oidcClientId: string;
  const copUrl: string;
  const tileServerUrl: string;
  const formSubmitNanoTdf: string;
  const keycloakDirectAuth: string;
  const kasUrl: string; // Temp
  const idpUrl: string; // Temp
}
