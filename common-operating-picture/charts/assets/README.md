This directory contains symlinks to `dsp-cop/db/` database
files, so that we can pack these files into the Chart.

For more information, see: 
    https://helm.sh/docs/chart_template_guide/accessing_files/